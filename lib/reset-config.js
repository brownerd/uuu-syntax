'use babel';

// Not in use

// export default function (pathToFile, message, encoding) {
//   return new Promise(
//     function (resolve, reject) {
//       fs.writeFile(pathToFile, message, encoding,
//         (err) => {console.log(err ? `Error: ${err}` : 'ok');}
//       );
//     }
//   )
// }
